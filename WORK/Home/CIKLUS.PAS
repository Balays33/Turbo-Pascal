Program ciklus;
   uses crt;
   var a,i,b,c:char;
    Begin
      clrscr;
      textbackground(0);
      clrscr;
      textcolor(4);
      for i:='a' to 'z' do
         begin
          write(I);
        end;
    readln;
    End.